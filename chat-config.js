window.VIDEO_CHAT_CONFIG = window.VIDEO_CHAT_CONFIG || {
  // GitHub Pages is static, so API calls must go through a public HTTPS server-side proxy.
  // You can also open video-chat.html?api=https://your-proxy.example.com/chat to store a proxy URL locally.
  // Never put a private model API key in this file before publishing.
  apiUrl: "",
  apiKey: "",
  endpointType: "",
  model: "deepseek-v4-pro"
};
