window.VIDEO_CHAT_CONFIG = window.VIDEO_CHAT_CONFIG || {
  // Public GitHub Pages builds should only point at a safe server-side proxy here.
  // Leave apiKey empty in this repository; provide private keys from a local wrapper if needed.
  apiUrl: "",
  apiKey: "",
  endpointType: "",
  model: "deepseek-v4-pro"
};
